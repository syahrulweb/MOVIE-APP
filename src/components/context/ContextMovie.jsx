// components/context/ContextMovie.jsx
import { createContext } from "react";

// Create context and set initial value
const MoviesContext = createContext(null);

// Export default MoviesContext
export default MoviesContext;
